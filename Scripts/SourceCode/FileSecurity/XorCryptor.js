/**  Cipher File with XOR */

function cipher(inputPath, outputPath, key, bufferSize, dKey, salt, hash, ipp)
{
XorCryptor.CipherFile(inputPath, outputPath, key, bufferSize, dKey, salt, hash, ipp);
}